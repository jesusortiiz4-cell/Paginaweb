backend:
  name: github
  repo: jesusortiiz4-cell/Paginaweb
  branch: main
  auth_type: implicit
  app_id: 2037367
  base_url: https://paginaweb-sand-five.vercel.app
  auth_endpoint: /admin/auth

media_folder: "static/uploads" # where uploaded files will go in the repo
public_folder: "/uploads"

collections:
  - name: "pages"
    label: "Pages"
    folder: "content/pages"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Body", name: "body", widget: "markdown" }

  - name: "posts"
    label: "Blog Posts"
    folder: "content/posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime" }
      - { label: "Body", name: "body", widget: "markdown" }
